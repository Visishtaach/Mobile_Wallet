import {
  StyleSheet,
  Text,
  View,
  ScrollView,
  TouchableOpacity,
} from 'react-native';
import React from 'react';

const Tokens = () => {
  return (
    <ScrollView>
      <TouchableOpacity>
        <Text>Tokens</Text>
      </TouchableOpacity>
      <TouchableOpacity>
        <Text>Tokens</Text>
      </TouchableOpacity>
    </ScrollView>
  );
};

export default Tokens;

const styles = StyleSheet.create({});
